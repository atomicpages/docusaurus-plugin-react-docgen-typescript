declare module '@theme/ReactPropTable' {
    const ReactPropTable: ({
        data,
    }: import('./theme/ReactPropTable').ReactPropTableProps) => JSX.Element | null;
    export default ReactPropTable;
}
